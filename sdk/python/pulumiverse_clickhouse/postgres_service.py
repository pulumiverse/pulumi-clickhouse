# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = ['PostgresServiceArgs', 'PostgresService']

@pulumi.input_type
class PostgresServiceArgs:
    def __init__(__self__, *,
                 cloud_provider: pulumi.Input[Optional[_builtins.str]] = None,
                 ha_type: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 password: pulumi.Input[Optional[_builtins.str]] = None,
                 pg_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 pgbouncer_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 postgres_version: pulumi.Input[Optional[_builtins.str]] = None,
                 read_replica_of: pulumi.Input[Optional[_builtins.str]] = None,
                 region: pulumi.Input[Optional[_builtins.str]] = None,
                 restore_to_point_in_time: pulumi.Input[Optional['PostgresServiceRestoreToPointInTimeArgs']] = None,
                 size: pulumi.Input[Optional[_builtins.str]] = None,
                 tags: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None):
        """
        The set of arguments for constructing a PostgresService resource.

        :param pulumi.Input[_builtins.str] cloud_provider: Cloud provider hosting the instance. Currently only 'aws' is supported. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] ha_type: High-availability mode. One of 'none' (single replica), 'async' (asynchronous replica), or 'sync' (synchronous replica). Mutable post-create; an HA flip triggers a transition. Omitting the attribute preserves the prior value (the server defaults to 'none' on Create); to actively downgrade, set 'ha_type = "none"' explicitly. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] name: Human-readable name. Immutable post-create; changes force destroy-and-recreate. Differs from clickhouse_service, which allows in-place rename.
        :param pulumi.Input[_builtins.str] password: Superuser password. Optional: set it to manage the password in Terraform, or omit it and the server generates one. Computed and refreshed from each GET (the server echoes it), so it always reflects the live password and an out-of-band rotation is reconciled on the next refresh. Changing this value rotates the password (PATCH /password). Must be ≥12 chars with at least one lowercase, one uppercase, and one digit. Stored in (sensitive) state.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pg_config: Postgres server parameters (pgConfig) as a key-value map. Declared parameters are the desired state — every apply sends the full map via POST /config (full replacement), so removing a key from the map removes it server-side. Set `pg_config = {}` to clear all parameters; omit the attribute to preserve the prior state (read replicas inherit the primary's parameters, and the server may surface values the configuration never declared — so it is Optional+Computed like tags). Out-of-band changes are reverted on the next apply. Some parameters require a database restart; the provider surfaces the server's restart-required hint as a warning (restart out-of-band).
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pgbouncer_config: PgBouncer connection-pooler parameters (pgBouncerConfig) as a key-value map. Same Optional+Computed semantics as pg_config; set `pgbouncer_config = {}` to clear.
        :param pulumi.Input[_builtins.str] postgres_version: Major Postgres version (e.g. '18'). The server picks the patch release within that major. Changing the major triggers destroy-and-recreate. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] read_replica_of: ID of the primary instance to replicate. When set, this instance is created as a read replica (streaming replication) of that primary. Immutable for a live replica: changing or removing it destroys and recreates the instance as a standalone primary. The one exception is an out-of-band promotion — if you promote the replica via the API/UI (is*primary becomes true), changing or removing read*replica*of then reconciles state in place without destroying the promoted primary. Mutually exclusive with restore*to*point*in_time and with password (a replica inherits the primary's superuser).
        :param pulumi.Input[_builtins.str] region: Cloud region (e.g. 'us-east-1'). No client-side validation; the server rejects unsupported regions. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input['PostgresServiceRestoreToPointInTimeArgs'] restore_to_point_in_time: Create this instance by restoring another Postgres instance's backup to a point in time. The whole block is create-time only: changing source*id / restore*target (re-restore to a new point) OR removing it both destroy and recreate the instance. The restored instance's name is this resource's top-level `name` and it is independent of its source. cloud*provider / region / postgres*version are inherited from the source — omit them, or set them to match (a mismatch is a plan-time error); size and ha*type must be omitted (the restored instance comes up at the backup's size and a server-assigned HA mode). Mutually exclusive with read*replica*of.
        :param pulumi.Input[_builtins.str] size: Instance size (VM SKU). See https://clickhouse.com/docs/cloud/managed-postgres/scaling for the supported instance families. No client-side enum; the server rejects unsupported sizes with HTTP 400 at apply time. Resizable in place. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] tags: Resource tags as a key-value map. Values must be non-empty (server's PATCH returns 400 on omitted value). Set `tags = {}` to clear all tags; omit the attribute to preserve the prior value.
        """
        if cloud_provider is not None:
            pulumi.set(__self__, "cloud_provider", cloud_provider)
        if ha_type is not None:
            pulumi.set(__self__, "ha_type", ha_type)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if password is not None:
            pulumi.set(__self__, "password", password)
        if pg_config is not None:
            pulumi.set(__self__, "pg_config", pg_config)
        if pgbouncer_config is not None:
            pulumi.set(__self__, "pgbouncer_config", pgbouncer_config)
        if postgres_version is not None:
            pulumi.set(__self__, "postgres_version", postgres_version)
        if read_replica_of is not None:
            pulumi.set(__self__, "read_replica_of", read_replica_of)
        if region is not None:
            pulumi.set(__self__, "region", region)
        if restore_to_point_in_time is not None:
            pulumi.set(__self__, "restore_to_point_in_time", restore_to_point_in_time)
        if size is not None:
            pulumi.set(__self__, "size", size)
        if tags is not None:
            pulumi.set(__self__, "tags", tags)

    @_builtins.property
    @pulumi.getter(name="cloudProvider")
    def cloud_provider(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Cloud provider hosting the instance. Currently only 'aws' is supported. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "cloud_provider")

    @cloud_provider.setter
    def cloud_provider(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "cloud_provider", value)

    @_builtins.property
    @pulumi.getter(name="haType")
    def ha_type(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        High-availability mode. One of 'none' (single replica), 'async' (asynchronous replica), or 'sync' (synchronous replica). Mutable post-create; an HA flip triggers a transition. Omitting the attribute preserves the prior value (the server defaults to 'none' on Create); to actively downgrade, set 'ha_type = "none"' explicitly. Omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "ha_type")

    @ha_type.setter
    def ha_type(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "ha_type", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Human-readable name. Immutable post-create; changes force destroy-and-recreate. Differs from clickhouse_service, which allows in-place rename.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def password(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Superuser password. Optional: set it to manage the password in Terraform, or omit it and the server generates one. Computed and refreshed from each GET (the server echoes it), so it always reflects the live password and an out-of-band rotation is reconciled on the next refresh. Changing this value rotates the password (PATCH /password). Must be ≥12 chars with at least one lowercase, one uppercase, and one digit. Stored in (sensitive) state.
        """
        return pulumi.get(self, "password")

    @password.setter
    def password(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "password", value)

    @_builtins.property
    @pulumi.getter(name="pgConfig")
    def pg_config(self) -> pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]:
        """
        Postgres server parameters (pgConfig) as a key-value map. Declared parameters are the desired state — every apply sends the full map via POST /config (full replacement), so removing a key from the map removes it server-side. Set `pg_config = {}` to clear all parameters; omit the attribute to preserve the prior state (read replicas inherit the primary's parameters, and the server may surface values the configuration never declared — so it is Optional+Computed like tags). Out-of-band changes are reverted on the next apply. Some parameters require a database restart; the provider surfaces the server's restart-required hint as a warning (restart out-of-band).
        """
        return pulumi.get(self, "pg_config")

    @pg_config.setter
    def pg_config(self, value: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "pg_config", value)

    @_builtins.property
    @pulumi.getter(name="pgbouncerConfig")
    def pgbouncer_config(self) -> pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]:
        """
        PgBouncer connection-pooler parameters (pgBouncerConfig) as a key-value map. Same Optional+Computed semantics as pg_config; set `pgbouncer_config = {}` to clear.
        """
        return pulumi.get(self, "pgbouncer_config")

    @pgbouncer_config.setter
    def pgbouncer_config(self, value: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "pgbouncer_config", value)

    @_builtins.property
    @pulumi.getter(name="postgresVersion")
    def postgres_version(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Major Postgres version (e.g. '18'). The server picks the patch release within that major. Changing the major triggers destroy-and-recreate. Omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "postgres_version")

    @postgres_version.setter
    def postgres_version(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "postgres_version", value)

    @_builtins.property
    @pulumi.getter(name="readReplicaOf")
    def read_replica_of(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        ID of the primary instance to replicate. When set, this instance is created as a read replica (streaming replication) of that primary. Immutable for a live replica: changing or removing it destroys and recreates the instance as a standalone primary. The one exception is an out-of-band promotion — if you promote the replica via the API/UI (is*primary becomes true), changing or removing read*replica*of then reconciles state in place without destroying the promoted primary. Mutually exclusive with restore*to*point*in_time and with password (a replica inherits the primary's superuser).
        """
        return pulumi.get(self, "read_replica_of")

    @read_replica_of.setter
    def read_replica_of(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "read_replica_of", value)

    @_builtins.property
    @pulumi.getter
    def region(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Cloud region (e.g. 'us-east-1'). No client-side validation; the server rejects unsupported regions. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "region")

    @region.setter
    def region(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "region", value)

    @_builtins.property
    @pulumi.getter(name="restoreToPointInTime")
    def restore_to_point_in_time(self) -> pulumi.Input[Optional['PostgresServiceRestoreToPointInTimeArgs']]:
        """
        Create this instance by restoring another Postgres instance's backup to a point in time. The whole block is create-time only: changing source*id / restore*target (re-restore to a new point) OR removing it both destroy and recreate the instance. The restored instance's name is this resource's top-level `name` and it is independent of its source. cloud*provider / region / postgres*version are inherited from the source — omit them, or set them to match (a mismatch is a plan-time error); size and ha*type must be omitted (the restored instance comes up at the backup's size and a server-assigned HA mode). Mutually exclusive with read*replica*of.
        """
        return pulumi.get(self, "restore_to_point_in_time")

    @restore_to_point_in_time.setter
    def restore_to_point_in_time(self, value: pulumi.Input[Optional['PostgresServiceRestoreToPointInTimeArgs']]):
        pulumi.set(self, "restore_to_point_in_time", value)

    @_builtins.property
    @pulumi.getter
    def size(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Instance size (VM SKU). See https://clickhouse.com/docs/cloud/managed-postgres/scaling for the supported instance families. No client-side enum; the server rejects unsupported sizes with HTTP 400 at apply time. Resizable in place. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "size")

    @size.setter
    def size(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "size", value)

    @_builtins.property
    @pulumi.getter
    def tags(self) -> pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]:
        """
        Resource tags as a key-value map. Values must be non-empty (server's PATCH returns 400 on omitted value). Set `tags = {}` to clear all tags; omit the attribute to preserve the prior value.
        """
        return pulumi.get(self, "tags")

    @tags.setter
    def tags(self, value: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "tags", value)


@pulumi.input_type
class _PostgresServiceState:
    def __init__(__self__, *,
                 cloud_provider: pulumi.Input[Optional[_builtins.str]] = None,
                 connection_string: pulumi.Input[Optional[_builtins.str]] = None,
                 created_at: pulumi.Input[Optional[_builtins.str]] = None,
                 ha_type: pulumi.Input[Optional[_builtins.str]] = None,
                 hostname: pulumi.Input[Optional[_builtins.str]] = None,
                 is_primary: pulumi.Input[Optional[_builtins.bool]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 password: pulumi.Input[Optional[_builtins.str]] = None,
                 pg_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 pgbouncer_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 port: pulumi.Input[Optional[_builtins.int]] = None,
                 postgres_version: pulumi.Input[Optional[_builtins.str]] = None,
                 read_replica_of: pulumi.Input[Optional[_builtins.str]] = None,
                 region: pulumi.Input[Optional[_builtins.str]] = None,
                 restore_to_point_in_time: pulumi.Input[Optional['PostgresServiceRestoreToPointInTimeArgs']] = None,
                 size: pulumi.Input[Optional[_builtins.str]] = None,
                 state: pulumi.Input[Optional[_builtins.str]] = None,
                 tags: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 username: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering PostgresService resources.

        :param pulumi.Input[_builtins.str] cloud_provider: Cloud provider hosting the instance. Currently only 'aws' is supported. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] connection_string: Full connection URI embedding the username and the password. Marked sensitive; the secret-redaction layer also covers it in TF_LOG=DEBUG output. Plan stability is managed in ModifyPlan: pinned to the prior value on unrelated updates, marked unknown when a password rotation is planned (it embeds the password).
        :param pulumi.Input[_builtins.str] created_at: RFC3339 timestamp when the service was created.
        :param pulumi.Input[_builtins.str] ha_type: High-availability mode. One of 'none' (single replica), 'async' (asynchronous replica), or 'sync' (synchronous replica). Mutable post-create; an HA flip triggers a transition. Omitting the attribute preserves the prior value (the server defaults to 'none' on Create); to actively downgrade, set 'ha_type = "none"' explicitly. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] hostname: Network hostname for client connections.
        :param pulumi.Input[_builtins.bool] is_primary: True when this instance is a writeable primary cluster; false only for separately-provisioned read replicas. HA standby servers (`ha_type = async`/`sync`) live inside the same primary instance and don't affect this value.
        :param pulumi.Input[_builtins.str] name: Human-readable name. Immutable post-create; changes force destroy-and-recreate. Differs from clickhouse_service, which allows in-place rename.
        :param pulumi.Input[_builtins.str] password: Superuser password. Optional: set it to manage the password in Terraform, or omit it and the server generates one. Computed and refreshed from each GET (the server echoes it), so it always reflects the live password and an out-of-band rotation is reconciled on the next refresh. Changing this value rotates the password (PATCH /password). Must be ≥12 chars with at least one lowercase, one uppercase, and one digit. Stored in (sensitive) state.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pg_config: Postgres server parameters (pgConfig) as a key-value map. Declared parameters are the desired state — every apply sends the full map via POST /config (full replacement), so removing a key from the map removes it server-side. Set `pg_config = {}` to clear all parameters; omit the attribute to preserve the prior state (read replicas inherit the primary's parameters, and the server may surface values the configuration never declared — so it is Optional+Computed like tags). Out-of-band changes are reverted on the next apply. Some parameters require a database restart; the provider surfaces the server's restart-required hint as a warning (restart out-of-band).
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pgbouncer_config: PgBouncer connection-pooler parameters (pgBouncerConfig) as a key-value map. Same Optional+Computed semantics as pg_config; set `pgbouncer_config = {}` to clear.
        :param pulumi.Input[_builtins.int] port: TCP port for client connections. Hardcoded to 5432 today; will become server-supplied once the API exposes a per-instance port field.
        :param pulumi.Input[_builtins.str] postgres_version: Major Postgres version (e.g. '18'). The server picks the patch release within that major. Changing the major triggers destroy-and-recreate. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] read_replica_of: ID of the primary instance to replicate. When set, this instance is created as a read replica (streaming replication) of that primary. Immutable for a live replica: changing or removing it destroys and recreates the instance as a standalone primary. The one exception is an out-of-band promotion — if you promote the replica via the API/UI (is*primary becomes true), changing or removing read*replica*of then reconciles state in place without destroying the promoted primary. Mutually exclusive with restore*to*point*in_time and with password (a replica inherits the primary's superuser).
        :param pulumi.Input[_builtins.str] region: Cloud region (e.g. 'us-east-1'). No client-side validation; the server rejects unsupported regions. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input['PostgresServiceRestoreToPointInTimeArgs'] restore_to_point_in_time: Create this instance by restoring another Postgres instance's backup to a point in time. The whole block is create-time only: changing source*id / restore*target (re-restore to a new point) OR removing it both destroy and recreate the instance. The restored instance's name is this resource's top-level `name` and it is independent of its source. cloud*provider / region / postgres*version are inherited from the source — omit them, or set them to match (a mismatch is a plan-time error); size and ha*type must be omitted (the restored instance comes up at the backup's size and a server-assigned HA mode). Mutually exclusive with read*replica*of.
        :param pulumi.Input[_builtins.str] size: Instance size (VM SKU). See https://clickhouse.com/docs/cloud/managed-postgres/scaling for the supported instance families. No client-side enum; the server rejects unsupported sizes with HTTP 400 at apply time. Resizable in place. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] state: Server-reported state. Examples: 'creating', 'running', 'restarting', 'unavailable', 'deleting'. Forward-compatible: unknown values from the server are surfaced verbatim.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] tags: Resource tags as a key-value map. Values must be non-empty (server's PATCH returns 400 on omitted value). Set `tags = {}` to clear all tags; omit the attribute to preserve the prior value.
        :param pulumi.Input[_builtins.str] username: Default superuser name.
        """
        if cloud_provider is not None:
            pulumi.set(__self__, "cloud_provider", cloud_provider)
        if connection_string is not None:
            pulumi.set(__self__, "connection_string", connection_string)
        if created_at is not None:
            pulumi.set(__self__, "created_at", created_at)
        if ha_type is not None:
            pulumi.set(__self__, "ha_type", ha_type)
        if hostname is not None:
            pulumi.set(__self__, "hostname", hostname)
        if is_primary is not None:
            pulumi.set(__self__, "is_primary", is_primary)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if password is not None:
            pulumi.set(__self__, "password", password)
        if pg_config is not None:
            pulumi.set(__self__, "pg_config", pg_config)
        if pgbouncer_config is not None:
            pulumi.set(__self__, "pgbouncer_config", pgbouncer_config)
        if port is not None:
            pulumi.set(__self__, "port", port)
        if postgres_version is not None:
            pulumi.set(__self__, "postgres_version", postgres_version)
        if read_replica_of is not None:
            pulumi.set(__self__, "read_replica_of", read_replica_of)
        if region is not None:
            pulumi.set(__self__, "region", region)
        if restore_to_point_in_time is not None:
            pulumi.set(__self__, "restore_to_point_in_time", restore_to_point_in_time)
        if size is not None:
            pulumi.set(__self__, "size", size)
        if state is not None:
            pulumi.set(__self__, "state", state)
        if tags is not None:
            pulumi.set(__self__, "tags", tags)
        if username is not None:
            pulumi.set(__self__, "username", username)

    @_builtins.property
    @pulumi.getter(name="cloudProvider")
    def cloud_provider(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Cloud provider hosting the instance. Currently only 'aws' is supported. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "cloud_provider")

    @cloud_provider.setter
    def cloud_provider(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "cloud_provider", value)

    @_builtins.property
    @pulumi.getter(name="connectionString")
    def connection_string(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Full connection URI embedding the username and the password. Marked sensitive; the secret-redaction layer also covers it in TF_LOG=DEBUG output. Plan stability is managed in ModifyPlan: pinned to the prior value on unrelated updates, marked unknown when a password rotation is planned (it embeds the password).
        """
        return pulumi.get(self, "connection_string")

    @connection_string.setter
    def connection_string(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "connection_string", value)

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        RFC3339 timestamp when the service was created.
        """
        return pulumi.get(self, "created_at")

    @created_at.setter
    def created_at(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "created_at", value)

    @_builtins.property
    @pulumi.getter(name="haType")
    def ha_type(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        High-availability mode. One of 'none' (single replica), 'async' (asynchronous replica), or 'sync' (synchronous replica). Mutable post-create; an HA flip triggers a transition. Omitting the attribute preserves the prior value (the server defaults to 'none' on Create); to actively downgrade, set 'ha_type = "none"' explicitly. Omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "ha_type")

    @ha_type.setter
    def ha_type(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "ha_type", value)

    @_builtins.property
    @pulumi.getter
    def hostname(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Network hostname for client connections.
        """
        return pulumi.get(self, "hostname")

    @hostname.setter
    def hostname(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "hostname", value)

    @_builtins.property
    @pulumi.getter(name="isPrimary")
    def is_primary(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        True when this instance is a writeable primary cluster; false only for separately-provisioned read replicas. HA standby servers (`ha_type = async`/`sync`) live inside the same primary instance and don't affect this value.
        """
        return pulumi.get(self, "is_primary")

    @is_primary.setter
    def is_primary(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "is_primary", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Human-readable name. Immutable post-create; changes force destroy-and-recreate. Differs from clickhouse_service, which allows in-place rename.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def password(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Superuser password. Optional: set it to manage the password in Terraform, or omit it and the server generates one. Computed and refreshed from each GET (the server echoes it), so it always reflects the live password and an out-of-band rotation is reconciled on the next refresh. Changing this value rotates the password (PATCH /password). Must be ≥12 chars with at least one lowercase, one uppercase, and one digit. Stored in (sensitive) state.
        """
        return pulumi.get(self, "password")

    @password.setter
    def password(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "password", value)

    @_builtins.property
    @pulumi.getter(name="pgConfig")
    def pg_config(self) -> pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]:
        """
        Postgres server parameters (pgConfig) as a key-value map. Declared parameters are the desired state — every apply sends the full map via POST /config (full replacement), so removing a key from the map removes it server-side. Set `pg_config = {}` to clear all parameters; omit the attribute to preserve the prior state (read replicas inherit the primary's parameters, and the server may surface values the configuration never declared — so it is Optional+Computed like tags). Out-of-band changes are reverted on the next apply. Some parameters require a database restart; the provider surfaces the server's restart-required hint as a warning (restart out-of-band).
        """
        return pulumi.get(self, "pg_config")

    @pg_config.setter
    def pg_config(self, value: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "pg_config", value)

    @_builtins.property
    @pulumi.getter(name="pgbouncerConfig")
    def pgbouncer_config(self) -> pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]:
        """
        PgBouncer connection-pooler parameters (pgBouncerConfig) as a key-value map. Same Optional+Computed semantics as pg_config; set `pgbouncer_config = {}` to clear.
        """
        return pulumi.get(self, "pgbouncer_config")

    @pgbouncer_config.setter
    def pgbouncer_config(self, value: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "pgbouncer_config", value)

    @_builtins.property
    @pulumi.getter
    def port(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        TCP port for client connections. Hardcoded to 5432 today; will become server-supplied once the API exposes a per-instance port field.
        """
        return pulumi.get(self, "port")

    @port.setter
    def port(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "port", value)

    @_builtins.property
    @pulumi.getter(name="postgresVersion")
    def postgres_version(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Major Postgres version (e.g. '18'). The server picks the patch release within that major. Changing the major triggers destroy-and-recreate. Omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "postgres_version")

    @postgres_version.setter
    def postgres_version(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "postgres_version", value)

    @_builtins.property
    @pulumi.getter(name="readReplicaOf")
    def read_replica_of(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        ID of the primary instance to replicate. When set, this instance is created as a read replica (streaming replication) of that primary. Immutable for a live replica: changing or removing it destroys and recreates the instance as a standalone primary. The one exception is an out-of-band promotion — if you promote the replica via the API/UI (is*primary becomes true), changing or removing read*replica*of then reconciles state in place without destroying the promoted primary. Mutually exclusive with restore*to*point*in_time and with password (a replica inherits the primary's superuser).
        """
        return pulumi.get(self, "read_replica_of")

    @read_replica_of.setter
    def read_replica_of(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "read_replica_of", value)

    @_builtins.property
    @pulumi.getter
    def region(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Cloud region (e.g. 'us-east-1'). No client-side validation; the server rejects unsupported regions. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "region")

    @region.setter
    def region(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "region", value)

    @_builtins.property
    @pulumi.getter(name="restoreToPointInTime")
    def restore_to_point_in_time(self) -> pulumi.Input[Optional['PostgresServiceRestoreToPointInTimeArgs']]:
        """
        Create this instance by restoring another Postgres instance's backup to a point in time. The whole block is create-time only: changing source*id / restore*target (re-restore to a new point) OR removing it both destroy and recreate the instance. The restored instance's name is this resource's top-level `name` and it is independent of its source. cloud*provider / region / postgres*version are inherited from the source — omit them, or set them to match (a mismatch is a plan-time error); size and ha*type must be omitted (the restored instance comes up at the backup's size and a server-assigned HA mode). Mutually exclusive with read*replica*of.
        """
        return pulumi.get(self, "restore_to_point_in_time")

    @restore_to_point_in_time.setter
    def restore_to_point_in_time(self, value: pulumi.Input[Optional['PostgresServiceRestoreToPointInTimeArgs']]):
        pulumi.set(self, "restore_to_point_in_time", value)

    @_builtins.property
    @pulumi.getter
    def size(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Instance size (VM SKU). See https://clickhouse.com/docs/cloud/managed-postgres/scaling for the supported instance families. No client-side enum; the server rejects unsupported sizes with HTTP 400 at apply time. Resizable in place. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "size")

    @size.setter
    def size(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "size", value)

    @_builtins.property
    @pulumi.getter
    def state(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Server-reported state. Examples: 'creating', 'running', 'restarting', 'unavailable', 'deleting'. Forward-compatible: unknown values from the server are surfaced verbatim.
        """
        return pulumi.get(self, "state")

    @state.setter
    def state(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "state", value)

    @_builtins.property
    @pulumi.getter
    def tags(self) -> pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]:
        """
        Resource tags as a key-value map. Values must be non-empty (server's PATCH returns 400 on omitted value). Set `tags = {}` to clear all tags; omit the attribute to preserve the prior value.
        """
        return pulumi.get(self, "tags")

    @tags.setter
    def tags(self, value: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "tags", value)

    @_builtins.property
    @pulumi.getter
    def username(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Default superuser name.
        """
        return pulumi.get(self, "username")

    @username.setter
    def username(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "username", value)


@pulumi.type_token("clickhouse:index/postgresService:PostgresService")
class PostgresService(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 cloud_provider: pulumi.Input[Optional[_builtins.str]] = None,
                 ha_type: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 password: pulumi.Input[Optional[_builtins.str]] = None,
                 pg_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 pgbouncer_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 postgres_version: pulumi.Input[Optional[_builtins.str]] = None,
                 read_replica_of: pulumi.Input[Optional[_builtins.str]] = None,
                 region: pulumi.Input[Optional[_builtins.str]] = None,
                 restore_to_point_in_time: pulumi.Input[Optional[Union['PostgresServiceRestoreToPointInTimeArgs', 'PostgresServiceRestoreToPointInTimeArgsDict']]] = None,
                 size: pulumi.Input[Optional[_builtins.str]] = None,
                 tags: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        """
        > **Note:** This resource is in alpha and its behavior may change in future provider versions.

        Manages a [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
        service. A Managed Postgres service is a fully-managed Postgres instance
        provisioned in the ClickHouse Cloud control plane.

        ## Supported lifecycle

        - Create — standard, as a read replica (`read_replica_of`), or by
          point-in-time restore (`restore_to_point_in_time`)
        - Read
        - Update — `size`, `ha_type`, `tags`, `pg_config`, `pgbouncer_config`,
          `password` rotation
        - Delete
        - Import

        Three companion data sources are also provided (alpha):
        `PostgresService`, `get_postgres_services`, and
        `get_postgres_service_ca_certificates`.

        ## Unsupported attributes

        The following are intentionally absent from the schema:

        - Operational commands (restart / promote / switchover). See
          "Operational commands" below for the rationale.
        - IP allowlist, private endpoints, backup configuration, maintenance
          windows, customer-managed encryption keys, BYOC. These depend on
          server-side endpoint additions.
        - Configurable lifecycle timeouts — there is no `timeouts {}` block; the
          provider uses fixed internal poll/retry budgets.

        ## Tag semantics

        Tags are a `map(string → string)` — same shape as `Service`.
        Values must be non-empty alphanumeric / `.` / `-` / `_` strings (server
        regex `^[a-zA-Z0-9._-]+$`); the server's PATCH endpoint returns `400
        BAD_REQUEST` on omitted values, so the schema rejects empty values at
        plan time.

        Setting `tags = {}` clears all user-controlled tags. Omitting the
        attribute entirely preserves the prior state value (`Optional +
        Computed + UseStateForUnknown`).

        The Postgres PATCH endpoint has PUT-like semantics specifically for the
        `tags` field: omitting it from the request body clears all tags
        server-side. The provider works around this by re-asserting the current
        state tags in every PATCH that mutates `size` or `ha_type`, so users
        won't lose tags when they resize or change HA mode. This is invisible
        end-to-end but worth knowing if you inspect `TF_LOG=DEBUG` request
        bodies — you'll see tags repeated on non-tag mutations.

        ## Example Usage

        ```python
        import pulumi
        import pulumiverse_clickhouse as clickhouse

        example = clickhouse.PostgresService("example",
            name="my-postgres",
            cloud_provider="aws",
            region="us-east-1",
            size="m6gd.large",
            ha_type="async",
            tags={
                "environment": "production",
                "team": "data",
            })
        ```

        ## Import

        The `pulumi import` command can be used, for example:

        !/bin/bash
        Managed Postgres services can be imported by specifying the service ID.
        The password is recovered on import (the server echoes it on GET) and stored
        in state.

        ```sh
        $ pulumi import clickhouse:index/postgresService:PostgresService example xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] cloud_provider: Cloud provider hosting the instance. Currently only 'aws' is supported. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] ha_type: High-availability mode. One of 'none' (single replica), 'async' (asynchronous replica), or 'sync' (synchronous replica). Mutable post-create; an HA flip triggers a transition. Omitting the attribute preserves the prior value (the server defaults to 'none' on Create); to actively downgrade, set 'ha_type = "none"' explicitly. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] name: Human-readable name. Immutable post-create; changes force destroy-and-recreate. Differs from clickhouse_service, which allows in-place rename.
        :param pulumi.Input[_builtins.str] password: Superuser password. Optional: set it to manage the password in Terraform, or omit it and the server generates one. Computed and refreshed from each GET (the server echoes it), so it always reflects the live password and an out-of-band rotation is reconciled on the next refresh. Changing this value rotates the password (PATCH /password). Must be ≥12 chars with at least one lowercase, one uppercase, and one digit. Stored in (sensitive) state.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pg_config: Postgres server parameters (pgConfig) as a key-value map. Declared parameters are the desired state — every apply sends the full map via POST /config (full replacement), so removing a key from the map removes it server-side. Set `pg_config = {}` to clear all parameters; omit the attribute to preserve the prior state (read replicas inherit the primary's parameters, and the server may surface values the configuration never declared — so it is Optional+Computed like tags). Out-of-band changes are reverted on the next apply. Some parameters require a database restart; the provider surfaces the server's restart-required hint as a warning (restart out-of-band).
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pgbouncer_config: PgBouncer connection-pooler parameters (pgBouncerConfig) as a key-value map. Same Optional+Computed semantics as pg_config; set `pgbouncer_config = {}` to clear.
        :param pulumi.Input[_builtins.str] postgres_version: Major Postgres version (e.g. '18'). The server picks the patch release within that major. Changing the major triggers destroy-and-recreate. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] read_replica_of: ID of the primary instance to replicate. When set, this instance is created as a read replica (streaming replication) of that primary. Immutable for a live replica: changing or removing it destroys and recreates the instance as a standalone primary. The one exception is an out-of-band promotion — if you promote the replica via the API/UI (is*primary becomes true), changing or removing read*replica*of then reconciles state in place without destroying the promoted primary. Mutually exclusive with restore*to*point*in_time and with password (a replica inherits the primary's superuser).
        :param pulumi.Input[_builtins.str] region: Cloud region (e.g. 'us-east-1'). No client-side validation; the server rejects unsupported regions. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[Union['PostgresServiceRestoreToPointInTimeArgs', 'PostgresServiceRestoreToPointInTimeArgsDict']] restore_to_point_in_time: Create this instance by restoring another Postgres instance's backup to a point in time. The whole block is create-time only: changing source*id / restore*target (re-restore to a new point) OR removing it both destroy and recreate the instance. The restored instance's name is this resource's top-level `name` and it is independent of its source. cloud*provider / region / postgres*version are inherited from the source — omit them, or set them to match (a mismatch is a plan-time error); size and ha*type must be omitted (the restored instance comes up at the backup's size and a server-assigned HA mode). Mutually exclusive with read*replica*of.
        :param pulumi.Input[_builtins.str] size: Instance size (VM SKU). See https://clickhouse.com/docs/cloud/managed-postgres/scaling for the supported instance families. No client-side enum; the server rejects unsupported sizes with HTTP 400 at apply time. Resizable in place. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] tags: Resource tags as a key-value map. Values must be non-empty (server's PATCH returns 400 on omitted value). Set `tags = {}` to clear all tags; omit the attribute to preserve the prior value.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: Optional[PostgresServiceArgs] = None,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        > **Note:** This resource is in alpha and its behavior may change in future provider versions.

        Manages a [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
        service. A Managed Postgres service is a fully-managed Postgres instance
        provisioned in the ClickHouse Cloud control plane.

        ## Supported lifecycle

        - Create — standard, as a read replica (`read_replica_of`), or by
          point-in-time restore (`restore_to_point_in_time`)
        - Read
        - Update — `size`, `ha_type`, `tags`, `pg_config`, `pgbouncer_config`,
          `password` rotation
        - Delete
        - Import

        Three companion data sources are also provided (alpha):
        `PostgresService`, `get_postgres_services`, and
        `get_postgres_service_ca_certificates`.

        ## Unsupported attributes

        The following are intentionally absent from the schema:

        - Operational commands (restart / promote / switchover). See
          "Operational commands" below for the rationale.
        - IP allowlist, private endpoints, backup configuration, maintenance
          windows, customer-managed encryption keys, BYOC. These depend on
          server-side endpoint additions.
        - Configurable lifecycle timeouts — there is no `timeouts {}` block; the
          provider uses fixed internal poll/retry budgets.

        ## Tag semantics

        Tags are a `map(string → string)` — same shape as `Service`.
        Values must be non-empty alphanumeric / `.` / `-` / `_` strings (server
        regex `^[a-zA-Z0-9._-]+$`); the server's PATCH endpoint returns `400
        BAD_REQUEST` on omitted values, so the schema rejects empty values at
        plan time.

        Setting `tags = {}` clears all user-controlled tags. Omitting the
        attribute entirely preserves the prior state value (`Optional +
        Computed + UseStateForUnknown`).

        The Postgres PATCH endpoint has PUT-like semantics specifically for the
        `tags` field: omitting it from the request body clears all tags
        server-side. The provider works around this by re-asserting the current
        state tags in every PATCH that mutates `size` or `ha_type`, so users
        won't lose tags when they resize or change HA mode. This is invisible
        end-to-end but worth knowing if you inspect `TF_LOG=DEBUG` request
        bodies — you'll see tags repeated on non-tag mutations.

        ## Example Usage

        ```python
        import pulumi
        import pulumiverse_clickhouse as clickhouse

        example = clickhouse.PostgresService("example",
            name="my-postgres",
            cloud_provider="aws",
            region="us-east-1",
            size="m6gd.large",
            ha_type="async",
            tags={
                "environment": "production",
                "team": "data",
            })
        ```

        ## Import

        The `pulumi import` command can be used, for example:

        !/bin/bash
        Managed Postgres services can be imported by specifying the service ID.
        The password is recovered on import (the server echoes it on GET) and stored
        in state.

        ```sh
        $ pulumi import clickhouse:index/postgresService:PostgresService example xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
        ```


        :param str resource_name: The name of the resource.
        :param PostgresServiceArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(PostgresServiceArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 cloud_provider: pulumi.Input[Optional[_builtins.str]] = None,
                 ha_type: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 password: pulumi.Input[Optional[_builtins.str]] = None,
                 pg_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 pgbouncer_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 postgres_version: pulumi.Input[Optional[_builtins.str]] = None,
                 read_replica_of: pulumi.Input[Optional[_builtins.str]] = None,
                 region: pulumi.Input[Optional[_builtins.str]] = None,
                 restore_to_point_in_time: pulumi.Input[Optional[Union['PostgresServiceRestoreToPointInTimeArgs', 'PostgresServiceRestoreToPointInTimeArgsDict']]] = None,
                 size: pulumi.Input[Optional[_builtins.str]] = None,
                 tags: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = PostgresServiceArgs.__new__(PostgresServiceArgs)

            __props__.__dict__["cloud_provider"] = cloud_provider
            __props__.__dict__["ha_type"] = ha_type
            __props__.__dict__["name"] = name
            __props__.__dict__["password"] = None if password is None else pulumi.Output.secret(password)
            __props__.__dict__["pg_config"] = pg_config
            __props__.__dict__["pgbouncer_config"] = pgbouncer_config
            __props__.__dict__["postgres_version"] = postgres_version
            __props__.__dict__["read_replica_of"] = read_replica_of
            __props__.__dict__["region"] = region
            __props__.__dict__["restore_to_point_in_time"] = restore_to_point_in_time
            __props__.__dict__["size"] = size
            __props__.__dict__["tags"] = tags
            __props__.__dict__["connection_string"] = None
            __props__.__dict__["created_at"] = None
            __props__.__dict__["hostname"] = None
            __props__.__dict__["is_primary"] = None
            __props__.__dict__["port"] = None
            __props__.__dict__["state"] = None
            __props__.__dict__["username"] = None
        secret_opts = pulumi.ResourceOptions(additional_secret_outputs=["connectionString", "password"])
        opts = pulumi.ResourceOptions.merge(opts, secret_opts)
        super(PostgresService, __self__).__init__(
            'clickhouse:index/postgresService:PostgresService',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            cloud_provider: pulumi.Input[Optional[_builtins.str]] = None,
            connection_string: pulumi.Input[Optional[_builtins.str]] = None,
            created_at: pulumi.Input[Optional[_builtins.str]] = None,
            ha_type: pulumi.Input[Optional[_builtins.str]] = None,
            hostname: pulumi.Input[Optional[_builtins.str]] = None,
            is_primary: pulumi.Input[Optional[_builtins.bool]] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            password: pulumi.Input[Optional[_builtins.str]] = None,
            pg_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
            pgbouncer_config: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
            port: pulumi.Input[Optional[_builtins.int]] = None,
            postgres_version: pulumi.Input[Optional[_builtins.str]] = None,
            read_replica_of: pulumi.Input[Optional[_builtins.str]] = None,
            region: pulumi.Input[Optional[_builtins.str]] = None,
            restore_to_point_in_time: pulumi.Input[Optional[Union['PostgresServiceRestoreToPointInTimeArgs', 'PostgresServiceRestoreToPointInTimeArgsDict']]] = None,
            size: pulumi.Input[Optional[_builtins.str]] = None,
            state: pulumi.Input[Optional[_builtins.str]] = None,
            tags: pulumi.Input[Optional[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
            username: pulumi.Input[Optional[_builtins.str]] = None) -> 'PostgresService':
        """
        Get an existing PostgresService resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] cloud_provider: Cloud provider hosting the instance. Currently only 'aws' is supported. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] connection_string: Full connection URI embedding the username and the password. Marked sensitive; the secret-redaction layer also covers it in TF_LOG=DEBUG output. Plan stability is managed in ModifyPlan: pinned to the prior value on unrelated updates, marked unknown when a password rotation is planned (it embeds the password).
        :param pulumi.Input[_builtins.str] created_at: RFC3339 timestamp when the service was created.
        :param pulumi.Input[_builtins.str] ha_type: High-availability mode. One of 'none' (single replica), 'async' (asynchronous replica), or 'sync' (synchronous replica). Mutable post-create; an HA flip triggers a transition. Omitting the attribute preserves the prior value (the server defaults to 'none' on Create); to actively downgrade, set 'ha_type = "none"' explicitly. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] hostname: Network hostname for client connections.
        :param pulumi.Input[_builtins.bool] is_primary: True when this instance is a writeable primary cluster; false only for separately-provisioned read replicas. HA standby servers (`ha_type = async`/`sync`) live inside the same primary instance and don't affect this value.
        :param pulumi.Input[_builtins.str] name: Human-readable name. Immutable post-create; changes force destroy-and-recreate. Differs from clickhouse_service, which allows in-place rename.
        :param pulumi.Input[_builtins.str] password: Superuser password. Optional: set it to manage the password in Terraform, or omit it and the server generates one. Computed and refreshed from each GET (the server echoes it), so it always reflects the live password and an out-of-band rotation is reconciled on the next refresh. Changing this value rotates the password (PATCH /password). Must be ≥12 chars with at least one lowercase, one uppercase, and one digit. Stored in (sensitive) state.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pg_config: Postgres server parameters (pgConfig) as a key-value map. Declared parameters are the desired state — every apply sends the full map via POST /config (full replacement), so removing a key from the map removes it server-side. Set `pg_config = {}` to clear all parameters; omit the attribute to preserve the prior state (read replicas inherit the primary's parameters, and the server may surface values the configuration never declared — so it is Optional+Computed like tags). Out-of-band changes are reverted on the next apply. Some parameters require a database restart; the provider surfaces the server's restart-required hint as a warning (restart out-of-band).
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] pgbouncer_config: PgBouncer connection-pooler parameters (pgBouncerConfig) as a key-value map. Same Optional+Computed semantics as pg_config; set `pgbouncer_config = {}` to clear.
        :param pulumi.Input[_builtins.int] port: TCP port for client connections. Hardcoded to 5432 today; will become server-supplied once the API exposes a per-instance port field.
        :param pulumi.Input[_builtins.str] postgres_version: Major Postgres version (e.g. '18'). The server picks the patch release within that major. Changing the major triggers destroy-and-recreate. Omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] read_replica_of: ID of the primary instance to replicate. When set, this instance is created as a read replica (streaming replication) of that primary. Immutable for a live replica: changing or removing it destroys and recreates the instance as a standalone primary. The one exception is an out-of-band promotion — if you promote the replica via the API/UI (is*primary becomes true), changing or removing read*replica*of then reconciles state in place without destroying the promoted primary. Mutually exclusive with restore*to*point*in_time and with password (a replica inherits the primary's superuser).
        :param pulumi.Input[_builtins.str] region: Cloud region (e.g. 'us-east-1'). No client-side validation; the server rejects unsupported regions. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[Union['PostgresServiceRestoreToPointInTimeArgs', 'PostgresServiceRestoreToPointInTimeArgsDict']] restore_to_point_in_time: Create this instance by restoring another Postgres instance's backup to a point in time. The whole block is create-time only: changing source*id / restore*target (re-restore to a new point) OR removing it both destroy and recreate the instance. The restored instance's name is this resource's top-level `name` and it is independent of its source. cloud*provider / region / postgres*version are inherited from the source — omit them, or set them to match (a mismatch is a plan-time error); size and ha*type must be omitted (the restored instance comes up at the backup's size and a server-assigned HA mode). Mutually exclusive with read*replica*of.
        :param pulumi.Input[_builtins.str] size: Instance size (VM SKU). See https://clickhouse.com/docs/cloud/managed-postgres/scaling for the supported instance families. No client-side enum; the server rejects unsupported sizes with HTTP 400 at apply time. Resizable in place. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        :param pulumi.Input[_builtins.str] state: Server-reported state. Examples: 'creating', 'running', 'restarting', 'unavailable', 'deleting'. Forward-compatible: unknown values from the server are surfaced verbatim.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] tags: Resource tags as a key-value map. Values must be non-empty (server's PATCH returns 400 on omitted value). Set `tags = {}` to clear all tags; omit the attribute to preserve the prior value.
        :param pulumi.Input[_builtins.str] username: Default superuser name.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _PostgresServiceState.__new__(_PostgresServiceState)

        __props__.__dict__["cloud_provider"] = cloud_provider
        __props__.__dict__["connection_string"] = connection_string
        __props__.__dict__["created_at"] = created_at
        __props__.__dict__["ha_type"] = ha_type
        __props__.__dict__["hostname"] = hostname
        __props__.__dict__["is_primary"] = is_primary
        __props__.__dict__["name"] = name
        __props__.__dict__["password"] = password
        __props__.__dict__["pg_config"] = pg_config
        __props__.__dict__["pgbouncer_config"] = pgbouncer_config
        __props__.__dict__["port"] = port
        __props__.__dict__["postgres_version"] = postgres_version
        __props__.__dict__["read_replica_of"] = read_replica_of
        __props__.__dict__["region"] = region
        __props__.__dict__["restore_to_point_in_time"] = restore_to_point_in_time
        __props__.__dict__["size"] = size
        __props__.__dict__["state"] = state
        __props__.__dict__["tags"] = tags
        __props__.__dict__["username"] = username
        return PostgresService(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="cloudProvider")
    def cloud_provider(self) -> pulumi.Output[_builtins.str]:
        """
        Cloud provider hosting the instance. Currently only 'aws' is supported. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "cloud_provider")

    @_builtins.property
    @pulumi.getter(name="connectionString")
    def connection_string(self) -> pulumi.Output[_builtins.str]:
        """
        Full connection URI embedding the username and the password. Marked sensitive; the secret-redaction layer also covers it in TF_LOG=DEBUG output. Plan stability is managed in ModifyPlan: pinned to the prior value on unrelated updates, marked unknown when a password rotation is planned (it embeds the password).
        """
        return pulumi.get(self, "connection_string")

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> pulumi.Output[_builtins.str]:
        """
        RFC3339 timestamp when the service was created.
        """
        return pulumi.get(self, "created_at")

    @_builtins.property
    @pulumi.getter(name="haType")
    def ha_type(self) -> pulumi.Output[_builtins.str]:
        """
        High-availability mode. One of 'none' (single replica), 'async' (asynchronous replica), or 'sync' (synchronous replica). Mutable post-create; an HA flip triggers a transition. Omitting the attribute preserves the prior value (the server defaults to 'none' on Create); to actively downgrade, set 'ha_type = "none"' explicitly. Omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "ha_type")

    @_builtins.property
    @pulumi.getter
    def hostname(self) -> pulumi.Output[_builtins.str]:
        """
        Network hostname for client connections.
        """
        return pulumi.get(self, "hostname")

    @_builtins.property
    @pulumi.getter(name="isPrimary")
    def is_primary(self) -> pulumi.Output[_builtins.bool]:
        """
        True when this instance is a writeable primary cluster; false only for separately-provisioned read replicas. HA standby servers (`ha_type = async`/`sync`) live inside the same primary instance and don't affect this value.
        """
        return pulumi.get(self, "is_primary")

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        Human-readable name. Immutable post-create; changes force destroy-and-recreate. Differs from clickhouse_service, which allows in-place rename.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def password(self) -> pulumi.Output[_builtins.str]:
        """
        Superuser password. Optional: set it to manage the password in Terraform, or omit it and the server generates one. Computed and refreshed from each GET (the server echoes it), so it always reflects the live password and an out-of-band rotation is reconciled on the next refresh. Changing this value rotates the password (PATCH /password). Must be ≥12 chars with at least one lowercase, one uppercase, and one digit. Stored in (sensitive) state.
        """
        return pulumi.get(self, "password")

    @_builtins.property
    @pulumi.getter(name="pgConfig")
    def pg_config(self) -> pulumi.Output[Mapping[str, _builtins.str]]:
        """
        Postgres server parameters (pgConfig) as a key-value map. Declared parameters are the desired state — every apply sends the full map via POST /config (full replacement), so removing a key from the map removes it server-side. Set `pg_config = {}` to clear all parameters; omit the attribute to preserve the prior state (read replicas inherit the primary's parameters, and the server may surface values the configuration never declared — so it is Optional+Computed like tags). Out-of-band changes are reverted on the next apply. Some parameters require a database restart; the provider surfaces the server's restart-required hint as a warning (restart out-of-band).
        """
        return pulumi.get(self, "pg_config")

    @_builtins.property
    @pulumi.getter(name="pgbouncerConfig")
    def pgbouncer_config(self) -> pulumi.Output[Mapping[str, _builtins.str]]:
        """
        PgBouncer connection-pooler parameters (pgBouncerConfig) as a key-value map. Same Optional+Computed semantics as pg_config; set `pgbouncer_config = {}` to clear.
        """
        return pulumi.get(self, "pgbouncer_config")

    @_builtins.property
    @pulumi.getter
    def port(self) -> pulumi.Output[_builtins.int]:
        """
        TCP port for client connections. Hardcoded to 5432 today; will become server-supplied once the API exposes a per-instance port field.
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter(name="postgresVersion")
    def postgres_version(self) -> pulumi.Output[_builtins.str]:
        """
        Major Postgres version (e.g. '18'). The server picks the patch release within that major. Changing the major triggers destroy-and-recreate. Omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "postgres_version")

    @_builtins.property
    @pulumi.getter(name="readReplicaOf")
    def read_replica_of(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        ID of the primary instance to replicate. When set, this instance is created as a read replica (streaming replication) of that primary. Immutable for a live replica: changing or removing it destroys and recreates the instance as a standalone primary. The one exception is an out-of-band promotion — if you promote the replica via the API/UI (is*primary becomes true), changing or removing read*replica*of then reconciles state in place without destroying the promoted primary. Mutually exclusive with restore*to*point*in_time and with password (a replica inherits the primary's superuser).
        """
        return pulumi.get(self, "read_replica_of")

    @_builtins.property
    @pulumi.getter
    def region(self) -> pulumi.Output[_builtins.str]:
        """
        Cloud region (e.g. 'us-east-1'). No client-side validation; the server rejects unsupported regions. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "region")

    @_builtins.property
    @pulumi.getter(name="restoreToPointInTime")
    def restore_to_point_in_time(self) -> pulumi.Output[Optional['outputs.PostgresServiceRestoreToPointInTime']]:
        """
        Create this instance by restoring another Postgres instance's backup to a point in time. The whole block is create-time only: changing source*id / restore*target (re-restore to a new point) OR removing it both destroy and recreate the instance. The restored instance's name is this resource's top-level `name` and it is independent of its source. cloud*provider / region / postgres*version are inherited from the source — omit them, or set them to match (a mismatch is a plan-time error); size and ha*type must be omitted (the restored instance comes up at the backup's size and a server-assigned HA mode). Mutually exclusive with read*replica*of.
        """
        return pulumi.get(self, "restore_to_point_in_time")

    @_builtins.property
    @pulumi.getter
    def size(self) -> pulumi.Output[_builtins.str]:
        """
        Instance size (VM SKU). See https://clickhouse.com/docs/cloud/managed-postgres/scaling for the supported instance families. No client-side enum; the server rejects unsupported sizes with HTTP 400 at apply time. Resizable in place. Required for a standard create; omit for a read replica or point-in-time restore (inherited from the source).
        """
        return pulumi.get(self, "size")

    @_builtins.property
    @pulumi.getter
    def state(self) -> pulumi.Output[_builtins.str]:
        """
        Server-reported state. Examples: 'creating', 'running', 'restarting', 'unavailable', 'deleting'. Forward-compatible: unknown values from the server are surfaced verbatim.
        """
        return pulumi.get(self, "state")

    @_builtins.property
    @pulumi.getter
    def tags(self) -> pulumi.Output[Mapping[str, _builtins.str]]:
        """
        Resource tags as a key-value map. Values must be non-empty (server's PATCH returns 400 on omitted value). Set `tags = {}` to clear all tags; omit the attribute to preserve the prior value.
        """
        return pulumi.get(self, "tags")

    @_builtins.property
    @pulumi.getter
    def username(self) -> pulumi.Output[_builtins.str]:
        """
        Default superuser name.
        """
        return pulumi.get(self, "username")

