# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs

__all__ = [
    'GetPostgresServicesResult',
    'AwaitableGetPostgresServicesResult',
    'get_postgres_services',
    'get_postgres_services_output',
]

@pulumi.output_type
class GetPostgresServicesResult:
    """
    A collection of values returned by getPostgresServices.
    """
    def __init__(__self__, id=None, services=None):
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if services and not isinstance(services, list):
            raise TypeError("Expected argument 'services' to be a list")
        pulumi.set(__self__, "services", services)

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The provider-assigned unique ID for this managed resource.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def services(self) -> Sequence['outputs.GetPostgresServicesServiceResult']:
        """
        All Managed Postgres services in the organization (summary fields).
        """
        return pulumi.get(self, "services")


class AwaitableGetPostgresServicesResult(GetPostgresServicesResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetPostgresServicesResult(
            id=self.id,
            services=self.services)


def get_postgres_services(opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetPostgresServicesResult:
    """
    > **Note:** This data source is in alpha and its behavior may change in future provider versions.

    Lists all [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
    services in the organization. Returns a `services` list of summary objects
    (`id`, `name`, `cloud_provider`, `region`, `postgres_version`, `size`,
    `ha_type`, `state`, `created_at`, `is_primary`).

    The list endpoint does not return `connection_string`, `password`, or
    `pg_config`; use the `PostgresService` data source (by ID) for the
    full set of attributes.

    ## Example

    ```python
    import pulumi
    import pulumi_clickhouse as clickhouse

    all = clickhouse.get_postgres_services()
    pulumi.export("serviceNames", [s.name for s in all.services])
    ```
    """
    __args__ = dict()
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('clickhouse:index/getPostgresServices:getPostgresServices', __args__, opts=opts, typ=GetPostgresServicesResult).value

    return AwaitableGetPostgresServicesResult(
        id=pulumi.get(__ret__, 'id'),
        services=pulumi.get(__ret__, 'services'))
def get_postgres_services_output(opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetPostgresServicesResult]:
    """
    > **Note:** This data source is in alpha and its behavior may change in future provider versions.

    Lists all [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
    services in the organization. Returns a `services` list of summary objects
    (`id`, `name`, `cloud_provider`, `region`, `postgres_version`, `size`,
    `ha_type`, `state`, `created_at`, `is_primary`).

    The list endpoint does not return `connection_string`, `password`, or
    `pg_config`; use the `PostgresService` data source (by ID) for the
    full set of attributes.

    ## Example

    ```python
    import pulumi
    import pulumi_clickhouse as clickhouse

    all = clickhouse.get_postgres_services()
    pulumi.export("serviceNames", [s.name for s in all.services])
    ```
    """
    __args__ = dict()
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('clickhouse:index/getPostgresServices:getPostgresServices', __args__, opts=opts, typ=GetPostgresServicesResult)
    return __ret__.apply(lambda __response__: GetPostgresServicesResult(
        id=pulumi.get(__response__, 'id'),
        services=pulumi.get(__response__, 'services')))
