# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['ServiceUpgradeWindowArgs', 'ServiceUpgradeWindow']

@pulumi.input_type
class ServiceUpgradeWindowArgs:
    def __init__(__self__, *,
                 service_id: pulumi.Input[_builtins.str],
                 start_hour_utc: pulumi.Input[_builtins.int],
                 weekday: pulumi.Input[_builtins.int]):
        """
        The set of arguments for constructing a ServiceUpgradeWindow resource.

        :param pulumi.Input[_builtins.str] service_id: ClickHouse Cloud service ID this upgrade window applies to.
        :param pulumi.Input[_builtins.int] start_hour_utc: UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
        :param pulumi.Input[_builtins.int] weekday: Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
        """
        pulumi.set(__self__, "service_id", service_id)
        pulumi.set(__self__, "start_hour_utc", start_hour_utc)
        pulumi.set(__self__, "weekday", weekday)

    @_builtins.property
    @pulumi.getter(name="serviceId")
    def service_id(self) -> pulumi.Input[_builtins.str]:
        """
        ClickHouse Cloud service ID this upgrade window applies to.
        """
        return pulumi.get(self, "service_id")

    @service_id.setter
    def service_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "service_id", value)

    @_builtins.property
    @pulumi.getter(name="startHourUtc")
    def start_hour_utc(self) -> pulumi.Input[_builtins.int]:
        """
        UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
        """
        return pulumi.get(self, "start_hour_utc")

    @start_hour_utc.setter
    def start_hour_utc(self, value: pulumi.Input[_builtins.int]):
        pulumi.set(self, "start_hour_utc", value)

    @_builtins.property
    @pulumi.getter
    def weekday(self) -> pulumi.Input[_builtins.int]:
        """
        Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
        """
        return pulumi.get(self, "weekday")

    @weekday.setter
    def weekday(self, value: pulumi.Input[_builtins.int]):
        pulumi.set(self, "weekday", value)


@pulumi.input_type
class _ServiceUpgradeWindowState:
    def __init__(__self__, *,
                 duration: pulumi.Input[Optional[_builtins.int]] = None,
                 service_id: pulumi.Input[Optional[_builtins.str]] = None,
                 start_hour_utc: pulumi.Input[Optional[_builtins.int]] = None,
                 weekday: pulumi.Input[Optional[_builtins.int]] = None):
        """
        Input properties used for looking up and filtering ServiceUpgradeWindow resources.

        :param pulumi.Input[_builtins.int] duration: Length of the upgrade window in hours. Server-controlled; currently always 6.
        :param pulumi.Input[_builtins.str] service_id: ClickHouse Cloud service ID this upgrade window applies to.
        :param pulumi.Input[_builtins.int] start_hour_utc: UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
        :param pulumi.Input[_builtins.int] weekday: Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
        """
        if duration is not None:
            pulumi.set(__self__, "duration", duration)
        if service_id is not None:
            pulumi.set(__self__, "service_id", service_id)
        if start_hour_utc is not None:
            pulumi.set(__self__, "start_hour_utc", start_hour_utc)
        if weekday is not None:
            pulumi.set(__self__, "weekday", weekday)

    @_builtins.property
    @pulumi.getter
    def duration(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        Length of the upgrade window in hours. Server-controlled; currently always 6.
        """
        return pulumi.get(self, "duration")

    @duration.setter
    def duration(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "duration", value)

    @_builtins.property
    @pulumi.getter(name="serviceId")
    def service_id(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        ClickHouse Cloud service ID this upgrade window applies to.
        """
        return pulumi.get(self, "service_id")

    @service_id.setter
    def service_id(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "service_id", value)

    @_builtins.property
    @pulumi.getter(name="startHourUtc")
    def start_hour_utc(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
        """
        return pulumi.get(self, "start_hour_utc")

    @start_hour_utc.setter
    def start_hour_utc(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "start_hour_utc", value)

    @_builtins.property
    @pulumi.getter
    def weekday(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
        """
        return pulumi.get(self, "weekday")

    @weekday.setter
    def weekday(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "weekday", value)


@pulumi.type_token("clickhouse:index/serviceUpgradeWindow:ServiceUpgradeWindow")
class ServiceUpgradeWindow(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 service_id: pulumi.Input[Optional[_builtins.str]] = None,
                 start_hour_utc: pulumi.Input[Optional[_builtins.int]] = None,
                 weekday: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        """
        You can use the *clickhouse_service_upgrade_window* resource to pin the weekly upgrade window for a ClickHouse Cloud service.

        When configured, the data plane only attempts service upgrades during the declared window. The window is a single weekly recurrence: a `weekday` plus a `start_hour_utc`. The window currently lasts 6 hours from `start_hour_utc`; `duration` is returned by the API and exposed as a read-only attribute.

        > **Note:** This resource is in alpha. Setting or updating an upgrade window requires the `canUseScheduledUpgrades` entitlement — reach out to ClickHouse support if the API returns `403 FORBIDDEN`. Deleting an upgrade window is allowed even after the entitlement is lost, so existing windows can always be cleared.

        ## Primary services only

        The upgrade window can only be set on primary services. Secondary services inherit the window from their primary. Applying this resource against a secondary service returns a `400` with the message `cannot set upgrade window on a secondary service`, surfaced by the provider as `Upgrade windows can only be set on primary services`.

        ## Allowed values

        - `weekday`: `0` (Sunday) – `6` (Saturday)
        - `start_hour_utc`: one of `0`, `6`, `12`, `18`

        ## Best-effort overwrite protection

        `Create` performs a `GET` before issuing the `PUT` so that an existing window (e.g. one configured out-of-band) surfaces a "please import" diagnostic instead of being silently overwritten. The check is best-effort: a window created between this resource's `GET` and `PUT` (e.g. by another tool, or another pulumi up) will still be overwritten by `PUT`.

        ## Example Usage

        ```python
        import pulumi
        import pulumiverse_clickhouse as clickhouse

        example = clickhouse.ServiceUpgradeWindow("example",
            service_id=example_clickhouse_service["id"],
            weekday=3,
            start_hour_utc=12)
        ```

        ## Import

        The `pulumi import` command can be used, for example:

        !/bin/bash
        Upgrade window can be imported by specifying the service ID.

        ```sh
        $ pulumi import clickhouse:index/serviceUpgradeWindow:ServiceUpgradeWindow example xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] service_id: ClickHouse Cloud service ID this upgrade window applies to.
        :param pulumi.Input[_builtins.int] start_hour_utc: UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
        :param pulumi.Input[_builtins.int] weekday: Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: ServiceUpgradeWindowArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        You can use the *clickhouse_service_upgrade_window* resource to pin the weekly upgrade window for a ClickHouse Cloud service.

        When configured, the data plane only attempts service upgrades during the declared window. The window is a single weekly recurrence: a `weekday` plus a `start_hour_utc`. The window currently lasts 6 hours from `start_hour_utc`; `duration` is returned by the API and exposed as a read-only attribute.

        > **Note:** This resource is in alpha. Setting or updating an upgrade window requires the `canUseScheduledUpgrades` entitlement — reach out to ClickHouse support if the API returns `403 FORBIDDEN`. Deleting an upgrade window is allowed even after the entitlement is lost, so existing windows can always be cleared.

        ## Primary services only

        The upgrade window can only be set on primary services. Secondary services inherit the window from their primary. Applying this resource against a secondary service returns a `400` with the message `cannot set upgrade window on a secondary service`, surfaced by the provider as `Upgrade windows can only be set on primary services`.

        ## Allowed values

        - `weekday`: `0` (Sunday) – `6` (Saturday)
        - `start_hour_utc`: one of `0`, `6`, `12`, `18`

        ## Best-effort overwrite protection

        `Create` performs a `GET` before issuing the `PUT` so that an existing window (e.g. one configured out-of-band) surfaces a "please import" diagnostic instead of being silently overwritten. The check is best-effort: a window created between this resource's `GET` and `PUT` (e.g. by another tool, or another pulumi up) will still be overwritten by `PUT`.

        ## Example Usage

        ```python
        import pulumi
        import pulumiverse_clickhouse as clickhouse

        example = clickhouse.ServiceUpgradeWindow("example",
            service_id=example_clickhouse_service["id"],
            weekday=3,
            start_hour_utc=12)
        ```

        ## Import

        The `pulumi import` command can be used, for example:

        !/bin/bash
        Upgrade window can be imported by specifying the service ID.

        ```sh
        $ pulumi import clickhouse:index/serviceUpgradeWindow:ServiceUpgradeWindow example xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
        ```


        :param str resource_name: The name of the resource.
        :param ServiceUpgradeWindowArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(ServiceUpgradeWindowArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 service_id: pulumi.Input[Optional[_builtins.str]] = None,
                 start_hour_utc: pulumi.Input[Optional[_builtins.int]] = None,
                 weekday: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = ServiceUpgradeWindowArgs.__new__(ServiceUpgradeWindowArgs)

            if service_id is None and not opts.urn:
                raise TypeError("Missing required property 'service_id'")
            __props__.__dict__["service_id"] = service_id
            if start_hour_utc is None and not opts.urn:
                raise TypeError("Missing required property 'start_hour_utc'")
            __props__.__dict__["start_hour_utc"] = start_hour_utc
            if weekday is None and not opts.urn:
                raise TypeError("Missing required property 'weekday'")
            __props__.__dict__["weekday"] = weekday
            __props__.__dict__["duration"] = None
        super(ServiceUpgradeWindow, __self__).__init__(
            'clickhouse:index/serviceUpgradeWindow:ServiceUpgradeWindow',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            duration: pulumi.Input[Optional[_builtins.int]] = None,
            service_id: pulumi.Input[Optional[_builtins.str]] = None,
            start_hour_utc: pulumi.Input[Optional[_builtins.int]] = None,
            weekday: pulumi.Input[Optional[_builtins.int]] = None) -> 'ServiceUpgradeWindow':
        """
        Get an existing ServiceUpgradeWindow resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.int] duration: Length of the upgrade window in hours. Server-controlled; currently always 6.
        :param pulumi.Input[_builtins.str] service_id: ClickHouse Cloud service ID this upgrade window applies to.
        :param pulumi.Input[_builtins.int] start_hour_utc: UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
        :param pulumi.Input[_builtins.int] weekday: Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _ServiceUpgradeWindowState.__new__(_ServiceUpgradeWindowState)

        __props__.__dict__["duration"] = duration
        __props__.__dict__["service_id"] = service_id
        __props__.__dict__["start_hour_utc"] = start_hour_utc
        __props__.__dict__["weekday"] = weekday
        return ServiceUpgradeWindow(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter
    def duration(self) -> pulumi.Output[_builtins.int]:
        """
        Length of the upgrade window in hours. Server-controlled; currently always 6.
        """
        return pulumi.get(self, "duration")

    @_builtins.property
    @pulumi.getter(name="serviceId")
    def service_id(self) -> pulumi.Output[_builtins.str]:
        """
        ClickHouse Cloud service ID this upgrade window applies to.
        """
        return pulumi.get(self, "service_id")

    @_builtins.property
    @pulumi.getter(name="startHourUtc")
    def start_hour_utc(self) -> pulumi.Output[_builtins.int]:
        """
        UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
        """
        return pulumi.get(self, "start_hour_utc")

    @_builtins.property
    @pulumi.getter
    def weekday(self) -> pulumi.Output[_builtins.int]:
        """
        Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
        """
        return pulumi.get(self, "weekday")

