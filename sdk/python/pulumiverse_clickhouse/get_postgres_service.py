# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = [
    'GetPostgresServiceResult',
    'AwaitableGetPostgresServiceResult',
    'get_postgres_service',
    'get_postgres_service_output',
]

@pulumi.output_type
class GetPostgresServiceResult:
    """
    A collection of values returned by getPostgresService.
    """
    def __init__(__self__, cloud_provider=None, connection_string=None, created_at=None, ha_type=None, hostname=None, id=None, is_primary=None, name=None, pg_config=None, pgbouncer_config=None, port=None, postgres_version=None, region=None, size=None, state=None, tags=None, username=None):
        if cloud_provider and not isinstance(cloud_provider, str):
            raise TypeError("Expected argument 'cloud_provider' to be a str")
        pulumi.set(__self__, "cloud_provider", cloud_provider)
        if connection_string and not isinstance(connection_string, str):
            raise TypeError("Expected argument 'connection_string' to be a str")
        pulumi.set(__self__, "connection_string", connection_string)
        if created_at and not isinstance(created_at, str):
            raise TypeError("Expected argument 'created_at' to be a str")
        pulumi.set(__self__, "created_at", created_at)
        if ha_type and not isinstance(ha_type, str):
            raise TypeError("Expected argument 'ha_type' to be a str")
        pulumi.set(__self__, "ha_type", ha_type)
        if hostname and not isinstance(hostname, str):
            raise TypeError("Expected argument 'hostname' to be a str")
        pulumi.set(__self__, "hostname", hostname)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if is_primary and not isinstance(is_primary, bool):
            raise TypeError("Expected argument 'is_primary' to be a bool")
        pulumi.set(__self__, "is_primary", is_primary)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if pg_config and not isinstance(pg_config, dict):
            raise TypeError("Expected argument 'pg_config' to be a dict")
        pulumi.set(__self__, "pg_config", pg_config)
        if pgbouncer_config and not isinstance(pgbouncer_config, dict):
            raise TypeError("Expected argument 'pgbouncer_config' to be a dict")
        pulumi.set(__self__, "pgbouncer_config", pgbouncer_config)
        if port and not isinstance(port, int):
            raise TypeError("Expected argument 'port' to be a int")
        pulumi.set(__self__, "port", port)
        if postgres_version and not isinstance(postgres_version, str):
            raise TypeError("Expected argument 'postgres_version' to be a str")
        pulumi.set(__self__, "postgres_version", postgres_version)
        if region and not isinstance(region, str):
            raise TypeError("Expected argument 'region' to be a str")
        pulumi.set(__self__, "region", region)
        if size and not isinstance(size, str):
            raise TypeError("Expected argument 'size' to be a str")
        pulumi.set(__self__, "size", size)
        if state and not isinstance(state, str):
            raise TypeError("Expected argument 'state' to be a str")
        pulumi.set(__self__, "state", state)
        if tags and not isinstance(tags, dict):
            raise TypeError("Expected argument 'tags' to be a dict")
        pulumi.set(__self__, "tags", tags)
        if username and not isinstance(username, str):
            raise TypeError("Expected argument 'username' to be a str")
        pulumi.set(__self__, "username", username)

    @_builtins.property
    @pulumi.getter(name="cloudProvider")
    def cloud_provider(self) -> _builtins.str:
        """
        Cloud provider hosting the instance.
        """
        return pulumi.get(self, "cloud_provider")

    @_builtins.property
    @pulumi.getter(name="connectionString")
    def connection_string(self) -> _builtins.str:
        """
        Full connection URI (embeds the password). Sensitive.
        """
        return pulumi.get(self, "connection_string")

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> _builtins.str:
        """
        RFC3339 creation timestamp.
        """
        return pulumi.get(self, "created_at")

    @_builtins.property
    @pulumi.getter(name="haType")
    def ha_type(self) -> _builtins.str:
        """
        High-availability mode ('none', 'async', 'sync').
        """
        return pulumi.get(self, "ha_type")

    @_builtins.property
    @pulumi.getter
    def hostname(self) -> _builtins.str:
        """
        Network hostname for client connections.
        """
        return pulumi.get(self, "hostname")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Unique identifier of the Postgres service to look up.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter(name="isPrimary")
    def is_primary(self) -> _builtins.bool:
        """
        True for a primary; false for a read replica.
        """
        return pulumi.get(self, "is_primary")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        Human-readable name.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="pgConfig")
    def pg_config(self) -> Mapping[str, _builtins.str]:
        """
        Postgres server parameters currently set on the instance. Read-only; a string map.
        """
        return pulumi.get(self, "pg_config")

    @_builtins.property
    @pulumi.getter(name="pgbouncerConfig")
    def pgbouncer_config(self) -> Mapping[str, _builtins.str]:
        """
        PgBouncer parameters currently set on the instance. Read-only; a string map.
        """
        return pulumi.get(self, "pgbouncer_config")

    @_builtins.property
    @pulumi.getter
    def port(self) -> _builtins.int:
        """
        TCP port for client connections.
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter(name="postgresVersion")
    def postgres_version(self) -> _builtins.str:
        """
        Major Postgres version.
        """
        return pulumi.get(self, "postgres_version")

    @_builtins.property
    @pulumi.getter
    def region(self) -> _builtins.str:
        """
        Cloud region.
        """
        return pulumi.get(self, "region")

    @_builtins.property
    @pulumi.getter
    def size(self) -> _builtins.str:
        """
        Instance size (VM SKU).
        """
        return pulumi.get(self, "size")

    @_builtins.property
    @pulumi.getter
    def state(self) -> _builtins.str:
        """
        Server-reported state.
        """
        return pulumi.get(self, "state")

    @_builtins.property
    @pulumi.getter
    def tags(self) -> Mapping[str, _builtins.str]:
        """
        User tags. Read-only; a string map.
        """
        return pulumi.get(self, "tags")

    @_builtins.property
    @pulumi.getter
    def username(self) -> _builtins.str:
        """
        Default superuser name.
        """
        return pulumi.get(self, "username")


class AwaitableGetPostgresServiceResult(GetPostgresServiceResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetPostgresServiceResult(
            cloud_provider=self.cloud_provider,
            connection_string=self.connection_string,
            created_at=self.created_at,
            ha_type=self.ha_type,
            hostname=self.hostname,
            id=self.id,
            is_primary=self.is_primary,
            name=self.name,
            pg_config=self.pg_config,
            pgbouncer_config=self.pgbouncer_config,
            port=self.port,
            postgres_version=self.postgres_version,
            region=self.region,
            size=self.size,
            state=self.state,
            tags=self.tags,
            username=self.username)


def get_postgres_service(id: Optional[_builtins.str] = None,
                         opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetPostgresServiceResult:
    """
    > **Note:** This data source is in alpha and its behavior may change in future provider versions.

    Fetches a single [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
    service by ID, including its current `pg_config` / `pgbouncer_config`.

    Returns the service's server-reported attributes: `cloud_provider`, `region`,
    `size`, `ha_type`, `postgres_version`, status (`state`, `created_at`,
    `is_primary`), connectivity (`hostname`, `username`, `connection_string`), and
    `tags` / `pg_config` / `pgbouncer_config` (`port` is the fixed default 5432 —
    the server doesn't expose a per-instance port). It does not surface a standalone
    `password` field, but the returned `connection_string` (sensitive) embeds the
    active password — so the credential is available here. The write-time create
    inputs — `read_replica_of`, `restore_to_point_in_time` — are not
    read back. `tags`, `pg_config`, and `pgbouncer_config` are read-only string maps.

    ## Example

    ```python
    import pulumi
    import pulumi_clickhouse as clickhouse

    example = clickhouse.get_postgres_service(id="5f1a3d9d-3d8e-8ed0-8d25-545aec9d5e3b")
    pulumi.export("host", example.hostname)
    ```


    :param _builtins.str id: Unique identifier of the Postgres service to look up.
    """
    __args__ = dict()
    __args__['id'] = id
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('clickhouse:index/getPostgresService:getPostgresService', __args__, opts=opts, typ=GetPostgresServiceResult).value

    return AwaitableGetPostgresServiceResult(
        cloud_provider=pulumi.get(__ret__, 'cloud_provider'),
        connection_string=pulumi.get(__ret__, 'connection_string'),
        created_at=pulumi.get(__ret__, 'created_at'),
        ha_type=pulumi.get(__ret__, 'ha_type'),
        hostname=pulumi.get(__ret__, 'hostname'),
        id=pulumi.get(__ret__, 'id'),
        is_primary=pulumi.get(__ret__, 'is_primary'),
        name=pulumi.get(__ret__, 'name'),
        pg_config=pulumi.get(__ret__, 'pg_config'),
        pgbouncer_config=pulumi.get(__ret__, 'pgbouncer_config'),
        port=pulumi.get(__ret__, 'port'),
        postgres_version=pulumi.get(__ret__, 'postgres_version'),
        region=pulumi.get(__ret__, 'region'),
        size=pulumi.get(__ret__, 'size'),
        state=pulumi.get(__ret__, 'state'),
        tags=pulumi.get(__ret__, 'tags'),
        username=pulumi.get(__ret__, 'username'))
def get_postgres_service_output(id: pulumi.Input[Optional[_builtins.str]] = None,
                                opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetPostgresServiceResult]:
    """
    > **Note:** This data source is in alpha and its behavior may change in future provider versions.

    Fetches a single [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
    service by ID, including its current `pg_config` / `pgbouncer_config`.

    Returns the service's server-reported attributes: `cloud_provider`, `region`,
    `size`, `ha_type`, `postgres_version`, status (`state`, `created_at`,
    `is_primary`), connectivity (`hostname`, `username`, `connection_string`), and
    `tags` / `pg_config` / `pgbouncer_config` (`port` is the fixed default 5432 —
    the server doesn't expose a per-instance port). It does not surface a standalone
    `password` field, but the returned `connection_string` (sensitive) embeds the
    active password — so the credential is available here. The write-time create
    inputs — `read_replica_of`, `restore_to_point_in_time` — are not
    read back. `tags`, `pg_config`, and `pgbouncer_config` are read-only string maps.

    ## Example

    ```python
    import pulumi
    import pulumi_clickhouse as clickhouse

    example = clickhouse.get_postgres_service(id="5f1a3d9d-3d8e-8ed0-8d25-545aec9d5e3b")
    pulumi.export("host", example.hostname)
    ```


    :param _builtins.str id: Unique identifier of the Postgres service to look up.
    """
    __args__ = dict()
    __args__['id'] = id
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('clickhouse:index/getPostgresService:getPostgresService', __args__, opts=opts, typ=GetPostgresServiceResult)
    return __ret__.apply(lambda __response__: GetPostgresServiceResult(
        cloud_provider=pulumi.get(__response__, 'cloud_provider'),
        connection_string=pulumi.get(__response__, 'connection_string'),
        created_at=pulumi.get(__response__, 'created_at'),
        ha_type=pulumi.get(__response__, 'ha_type'),
        hostname=pulumi.get(__response__, 'hostname'),
        id=pulumi.get(__response__, 'id'),
        is_primary=pulumi.get(__response__, 'is_primary'),
        name=pulumi.get(__response__, 'name'),
        pg_config=pulumi.get(__response__, 'pg_config'),
        pgbouncer_config=pulumi.get(__response__, 'pgbouncer_config'),
        port=pulumi.get(__response__, 'port'),
        postgres_version=pulumi.get(__response__, 'postgres_version'),
        region=pulumi.get(__response__, 'region'),
        size=pulumi.get(__response__, 'size'),
        state=pulumi.get(__response__, 'state'),
        tags=pulumi.get(__response__, 'tags'),
        username=pulumi.get(__response__, 'username')))
