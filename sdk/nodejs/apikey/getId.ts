// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * The *clickhouse_api_key_id* data source can be used to retrieve the UUID of a ClickHouse cloud API key.
 * It is meant to be used in the *clickhouse_service* resource to set the `queryApiEndpoints` attribute.
 *
 * It can be used in two ways:
 *
 * 1) To retrieve information about an API Key, by providing its name
 * 2) To retrieve information about the API Key currently configured for running the terraform provider
 *
 * In both cases the data source will contain the `id` and `name` attributes.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const myApiKey = clickhouse.ApiKey.getId({
 *     name: "my-api-key",
 * });
 * ```
 */
export function getId(args?: GetIdArgs, opts?: pulumi.InvokeOptions): Promise<GetIdResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("clickhouse:ApiKey/getId:getId", {
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getId.
 */
export interface GetIdArgs {
    /**
     * The name of the API key to retrieve information about. If left empty, the API key used by the Terraform provider is used instead.
     */
    name?: string;
}

/**
 * A collection of values returned by getId.
 */
export interface GetIdResult {
    /**
     * The ID of the API key used by the provider to connect to the service. This is a read-only attribute.
     */
    readonly id: string;
    /**
     * The name of the API key to retrieve information about. If left empty, the API key used by the Terraform provider is used instead.
     */
    readonly name: string;
}
/**
 * The *clickhouse_api_key_id* data source can be used to retrieve the UUID of a ClickHouse cloud API key.
 * It is meant to be used in the *clickhouse_service* resource to set the `queryApiEndpoints` attribute.
 *
 * It can be used in two ways:
 *
 * 1) To retrieve information about an API Key, by providing its name
 * 2) To retrieve information about the API Key currently configured for running the terraform provider
 *
 * In both cases the data source will contain the `id` and `name` attributes.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const myApiKey = clickhouse.ApiKey.getId({
 *     name: "my-api-key",
 * });
 * ```
 */
export function getIdOutput(args?: GetIdOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetIdResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("clickhouse:ApiKey/getId:getId", {
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getId.
 */
export interface GetIdOutputArgs {
    /**
     * The name of the API key to retrieve information about. If left empty, the API key used by the Terraform provider is used instead.
     */
    name?: pulumi.Input<string>;
}
