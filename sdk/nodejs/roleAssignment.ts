// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Use the *clickhouse_role_assignment* resource to assign actors (users and/or API keys) to a role. Works for both system roles and custom roles.
 *
 * One resource manages all actors for a given role. Use `userIds` to assign users, `apiKeyIds` to assign API keys, or both. Only one `clickhouse.RoleAssignment` per role is needed.
 *
 * > **Note:** On delete, all actors are removed from the role.
 *
 * To look up the ID of a system role by name, use the `clickhouse.Role` data source.
 * To look up a user ID by email, use the `clickhouse.getUser` data source.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const member = clickhouse.getRole({
 *     name: "Member",
 * });
 * const alice = clickhouse.getUser({
 *     email: "alice@example.com",
 * });
 * const bob = clickhouse.getUser({
 *     email: "bob@example.com",
 * });
 * const current = clickhouse.ApiKey.getId({});
 * const memberRoleAssignment = new clickhouse.RoleAssignment("member", {
 *     roleId: member.then(member => member.id),
 *     userIds: [
 *         alice.then(alice => alice.id),
 *         bob.then(bob => bob.id),
 *     ],
 *     apiKeyIds: [current.then(current => current.id)],
 * });
 * ```
 *
 * ### Additional Examples
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const member = clickhouse.getRole({
 *     name: "Member",
 * });
 * const alice = clickhouse.getUser({
 *     email: "alice@example.com",
 * });
 * const current = clickhouse.ApiKey.getId({});
 * const memberRoleAssignment = new clickhouse.RoleAssignment("member", {
 *     roleId: member.then(member => member.id),
 *     userIds: [alice.then(alice => alice.id)],
 *     apiKeyIds: [current.then(current => current.id)],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * !/bin/bash
 * Role assignments can be imported by specifying the role ID.
 *
 * ```sh
 * $ pulumi import clickhouse:index/roleAssignment:RoleAssignment example xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
 * ```
 */
export class RoleAssignment extends pulumi.CustomResource {
    /**
     * Get an existing RoleAssignment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RoleAssignmentState, opts?: pulumi.CustomResourceOptions): RoleAssignment {
        return new RoleAssignment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'clickhouse:index/roleAssignment:RoleAssignment';

    /**
     * Returns true if the given object is an instance of RoleAssignment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RoleAssignment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RoleAssignment.__pulumiType;
    }

    /**
     * Set of API key IDs to assign to the role.
     */
    declare public readonly apiKeyIds: pulumi.Output<string[] | undefined>;
    /**
     * ID of the role to assign actors to.
     */
    declare public readonly roleId: pulumi.Output<string>;
    /**
     * Set of user IDs to assign to the role.
     */
    declare public readonly userIds: pulumi.Output<string[] | undefined>;

    /**
     * Create a RoleAssignment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RoleAssignmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RoleAssignmentArgs | RoleAssignmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RoleAssignmentState | undefined;
            resourceInputs["apiKeyIds"] = state?.apiKeyIds;
            resourceInputs["roleId"] = state?.roleId;
            resourceInputs["userIds"] = state?.userIds;
        } else {
            const args = argsOrState as RoleAssignmentArgs | undefined;
            if (args?.roleId === undefined && !opts.urn) {
                throw new Error("Missing required property 'roleId'");
            }
            resourceInputs["apiKeyIds"] = args?.apiKeyIds;
            resourceInputs["roleId"] = args?.roleId;
            resourceInputs["userIds"] = args?.userIds;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RoleAssignment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RoleAssignment resources.
 */
export interface RoleAssignmentState {
    /**
     * Set of API key IDs to assign to the role.
     */
    apiKeyIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * ID of the role to assign actors to.
     */
    roleId?: pulumi.Input<string | undefined>;
    /**
     * Set of user IDs to assign to the role.
     */
    userIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a RoleAssignment resource.
 */
export interface RoleAssignmentArgs {
    /**
     * Set of API key IDs to assign to the role.
     */
    apiKeyIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * ID of the role to assign actors to.
     */
    roleId: pulumi.Input<string>;
    /**
     * Set of user IDs to assign to the role.
     */
    userIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}
