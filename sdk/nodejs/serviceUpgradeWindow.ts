// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * You can use the *clickhouse_service_upgrade_window* resource to pin the weekly upgrade window for a ClickHouse Cloud service.
 *
 * When configured, the data plane only attempts service upgrades during the declared window. The window is a single weekly recurrence: a `weekday` plus a `startHourUtc`. The window currently lasts 6 hours from `startHourUtc`; `duration` is returned by the API and exposed as a read-only attribute.
 *
 * > **Note:** This resource is in alpha. Setting or updating an upgrade window requires the `canUseScheduledUpgrades` entitlement — reach out to ClickHouse support if the API returns `403 FORBIDDEN`. Deleting an upgrade window is allowed even after the entitlement is lost, so existing windows can always be cleared.
 *
 * ## Primary services only
 *
 * The upgrade window can only be set on primary services. Secondary services inherit the window from their primary. Applying this resource against a secondary service returns a `400` with the message `cannot set upgrade window on a secondary service`, surfaced by the provider as `Upgrade windows can only be set on primary services`.
 *
 * ## Allowed values
 *
 * - `weekday`: `0` (Sunday) – `6` (Saturday)
 * - `startHourUtc`: one of `0`, `6`, `12`, `18`
 *
 * ## Best-effort overwrite protection
 *
 * `Create` performs a `GET` before issuing the `PUT` so that an existing window (e.g. one configured out-of-band) surfaces a "please import" diagnostic instead of being silently overwritten. The check is best-effort: a window created between this resource's `GET` and `PUT` (e.g. by another tool, or another pulumi up) will still be overwritten by `PUT`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const example = new clickhouse.ServiceUpgradeWindow("example", {
 *     serviceId: exampleClickhouseService.id,
 *     weekday: 3,
 *     startHourUtc: 12,
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * !/bin/bash
 * Upgrade window can be imported by specifying the service ID.
 *
 * ```sh
 * $ pulumi import clickhouse:index/serviceUpgradeWindow:ServiceUpgradeWindow example xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
 * ```
 */
export class ServiceUpgradeWindow extends pulumi.CustomResource {
    /**
     * Get an existing ServiceUpgradeWindow resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ServiceUpgradeWindowState, opts?: pulumi.CustomResourceOptions): ServiceUpgradeWindow {
        return new ServiceUpgradeWindow(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'clickhouse:index/serviceUpgradeWindow:ServiceUpgradeWindow';

    /**
     * Returns true if the given object is an instance of ServiceUpgradeWindow.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ServiceUpgradeWindow {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ServiceUpgradeWindow.__pulumiType;
    }

    /**
     * Length of the upgrade window in hours. Server-controlled; currently always 6.
     */
    declare public /*out*/ readonly duration: pulumi.Output<number>;
    /**
     * ClickHouse Cloud service ID this upgrade window applies to.
     */
    declare public readonly serviceId: pulumi.Output<string>;
    /**
     * UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
     */
    declare public readonly startHourUtc: pulumi.Output<number>;
    /**
     * Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
     */
    declare public readonly weekday: pulumi.Output<number>;

    /**
     * Create a ServiceUpgradeWindow resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ServiceUpgradeWindowArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ServiceUpgradeWindowArgs | ServiceUpgradeWindowState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ServiceUpgradeWindowState | undefined;
            resourceInputs["duration"] = state?.duration;
            resourceInputs["serviceId"] = state?.serviceId;
            resourceInputs["startHourUtc"] = state?.startHourUtc;
            resourceInputs["weekday"] = state?.weekday;
        } else {
            const args = argsOrState as ServiceUpgradeWindowArgs | undefined;
            if (args?.serviceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'serviceId'");
            }
            if (args?.startHourUtc === undefined && !opts.urn) {
                throw new Error("Missing required property 'startHourUtc'");
            }
            if (args?.weekday === undefined && !opts.urn) {
                throw new Error("Missing required property 'weekday'");
            }
            resourceInputs["serviceId"] = args?.serviceId;
            resourceInputs["startHourUtc"] = args?.startHourUtc;
            resourceInputs["weekday"] = args?.weekday;
            resourceInputs["duration"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ServiceUpgradeWindow.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ServiceUpgradeWindow resources.
 */
export interface ServiceUpgradeWindowState {
    /**
     * Length of the upgrade window in hours. Server-controlled; currently always 6.
     */
    duration?: pulumi.Input<number | undefined>;
    /**
     * ClickHouse Cloud service ID this upgrade window applies to.
     */
    serviceId?: pulumi.Input<string | undefined>;
    /**
     * UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
     */
    startHourUtc?: pulumi.Input<number | undefined>;
    /**
     * Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
     */
    weekday?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a ServiceUpgradeWindow resource.
 */
export interface ServiceUpgradeWindowArgs {
    /**
     * ClickHouse Cloud service ID this upgrade window applies to.
     */
    serviceId: pulumi.Input<string>;
    /**
     * UTC hour when the upgrade window starts. Must be one of 0, 6, 12, or 18.
     */
    startHourUtc: pulumi.Input<number>;
    /**
     * Day of the week the upgrade window starts. 0 = Sunday, 1 = Monday, …, 6 = Saturday.
     */
    weekday: pulumi.Input<number>;
}
