// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Note:** This data source is in alpha and its behavior may change in future provider versions.
 *
 * Lists all [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
 * services in the organization. Returns a `services` list of summary objects
 * (`id`, `name`, `cloudProvider`, `region`, `postgresVersion`, `size`,
 * `haType`, `state`, `createdAt`, `isPrimary`).
 *
 * The list endpoint does not return `connectionString`, `password`, or
 * `pgConfig`; use the `clickhouse.PostgresService` data source (by ID) for the
 * full set of attributes.
 *
 * ## Example
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const all = clickhouse.getPostgresServices({});
 * export const serviceNames = all.then(all => .map(s => (s.name)));
 * ```
 */
export function getPostgresServices(opts?: pulumi.InvokeOptions): Promise<GetPostgresServicesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("clickhouse:index/getPostgresServices:getPostgresServices", {
    }, opts);
}

/**
 * A collection of values returned by getPostgresServices.
 */
export interface GetPostgresServicesResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * All Managed Postgres services in the organization (summary fields).
     */
    readonly services: outputs.GetPostgresServicesService[];
}
/**
 * > **Note:** This data source is in alpha and its behavior may change in future provider versions.
 *
 * Lists all [ClickHouse Cloud Managed Postgres](https://clickhouse.com/cloud/postgres)
 * services in the organization. Returns a `services` list of summary objects
 * (`id`, `name`, `cloudProvider`, `region`, `postgresVersion`, `size`,
 * `haType`, `state`, `createdAt`, `isPrimary`).
 *
 * The list endpoint does not return `connectionString`, `password`, or
 * `pgConfig`; use the `clickhouse.PostgresService` data source (by ID) for the
 * full set of attributes.
 *
 * ## Example
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const all = clickhouse.getPostgresServices({});
 * export const serviceNames = all.then(all => .map(s => (s.name)));
 * ```
 */
export function getPostgresServicesOutput(opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetPostgresServicesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("clickhouse:index/getPostgresServices:getPostgresServices", {
    }, opts);
}
