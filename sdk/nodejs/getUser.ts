// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Use this data source to look up an organization member by email or user ID. Exactly one of `email` or `id` must be set.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const alice = clickhouse.getUser({
 *     email: "alice@example.com",
 * });
 * const member = clickhouse.getRole({
 *     name: "Member",
 * });
 * const aliceMember = new clickhouse.RoleAssignment("alice_member", {
 *     roleId: member.then(member => member.id),
 *     userIds: [alice.then(alice => alice.id)],
 * });
 * ```
 */
export function getUser(args?: GetUserArgs, opts?: pulumi.InvokeOptions): Promise<GetUserResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("clickhouse:index/getUser:getUser", {
        "email": args.email,
        "id": args.id,
    }, opts);
}

/**
 * A collection of arguments for invoking getUser.
 */
export interface GetUserArgs {
    /**
     * Email address of the user. Exactly one of id or email must be set.
     */
    email?: string;
    /**
     * User ID. Exactly one of id or email must be set.
     */
    id?: string;
}

/**
 * A collection of values returned by getUser.
 */
export interface GetUserResult {
    /**
     * Roles assigned to the user.
     */
    readonly assignedRoles: outputs.GetUserAssignedRole[];
    /**
     * Email address of the user. Exactly one of id or email must be set.
     */
    readonly email: string;
    /**
     * User ID. Exactly one of id or email must be set.
     */
    readonly id: string;
    /**
     * Timestamp when the user joined the organization.
     */
    readonly joinedAt: string;
    /**
     * Display name of the user.
     */
    readonly name: string;
}
/**
 * Use this data source to look up an organization member by email or user ID. Exactly one of `email` or `id` must be set.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as clickhouse from "@pulumiverse/clickhouse";
 *
 * const alice = clickhouse.getUser({
 *     email: "alice@example.com",
 * });
 * const member = clickhouse.getRole({
 *     name: "Member",
 * });
 * const aliceMember = new clickhouse.RoleAssignment("alice_member", {
 *     roleId: member.then(member => member.id),
 *     userIds: [alice.then(alice => alice.id)],
 * });
 * ```
 */
export function getUserOutput(args?: GetUserOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetUserResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("clickhouse:index/getUser:getUser", {
        "email": args.email,
        "id": args.id,
    }, opts);
}

/**
 * A collection of arguments for invoking getUser.
 */
export interface GetUserOutputArgs {
    /**
     * Email address of the user. Exactly one of id or email must be set.
     */
    email?: pulumi.Input<string | undefined>;
    /**
     * User ID. Exactly one of id or email must be set.
     */
    id?: pulumi.Input<string | undefined>;
}
