version: "2"
formatters:
  enable:
    - gofmt
linters:
  enable:
    - errcheck
    - goconst
    - gosec
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - unconvert
    - staticcheck
    - unused
  enable-all: false
run:
  timeout: 20m
issues:
  exclude-files:
    - schema.go
    - pulumiManifest.go
